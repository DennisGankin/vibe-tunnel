# Downloaded VS Code CLI binary (setup.sh puts it here on the cluster)
cli/code
cli/*.tar.gz

.DS_Store

# Built container images and the runtime sandbox homes (logins, VS Code server, ...)
images/*.sif
home/

# Lab-specific defaults of an installation (see site.env.example)
site.env
